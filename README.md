This slack bot has two unique commands:
-Riddle
-Guess
to run these commands go into the bot spam slack channel and type in the following:
for command #1:
  /the-goat-riddle -- displays one of the two pre defined riddle
for command #2: 
  /the-goat-guess [your guess] -- compares the users guess with the current riddles answers 
